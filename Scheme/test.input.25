(define x 23)

(letrec ((x 5) (y (lambda () (+ 2 x)))) y)
; should print func

(letrec ((x 5) (y (lambda () (+ 2 x)))) (y))
; should print 7

(letrec ((y 2)
		 (z (lambda (y) y))) (z 0))
; should print 0

(letrec ((y 2)
		 (z (lambda (m n) y))) (z 0 2))
; should print 2

(let ((z 10)) (+ z x))
; should print 33