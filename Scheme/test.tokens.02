  paren : (
 symbol : define
 symbol : valid-bst?
  paren : (
 symbol : lambda
  paren : (
 symbol : bst
  paren : )
  paren : (
 symbol : if
  paren : (
 symbol : list?
 symbol : bst
  paren : )
  paren : (
 symbol : if
  paren : (
 symbol : null-bst?
 symbol : bst
  paren : )
boolean : #t
  paren : (
 symbol : if
  paren : (
 symbol : and
  paren : (
 symbol : list?
  paren : (
 symbol : left
 symbol : bst
  paren : )
  paren : )
  paren : (
 symbol : list?
  paren : (
 symbol : right
 symbol : bst
  paren : )
  paren : )
  paren : )
boolean : #t
boolean : #f
  paren : )
  paren : )
boolean : #f
  paren : )
  paren : )
  paren : )
